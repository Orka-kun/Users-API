{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install --force && npm run build && cd ../server && npm install",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": "22.x"
  }
}
