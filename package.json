{
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": "22.x"
  }
}
